---
import ArchiveView from '@/components/ArchiveView.astro';
import { getPosts, paginate } from '@/lib/posts';

const posts = await getPosts();
const page = paginate(posts, 1);
---

<ArchiveView page={page} total={posts.length} />
